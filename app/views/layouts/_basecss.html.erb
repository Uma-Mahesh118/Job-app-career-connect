<script>
    function reloadDiv() {
      $.ajax({
        url: "messages_path",
        success: function(response) {
          $("#change").html(response); // Update the content of the div with the response
        }
      });
    }

    // Function to reload the div every 10 seconds
    function startReload() {
      setInterval(reloadDiv, 10000); // 10,000 milliseconds = 10 seconds
    }
</script>
<style>

      .navbar { background-color: dodgerblue !important; }
      .navbar-custom {
        height: 73px;
      }
      .nav-link { color: white !important}
      #logo { float: left; padding-left:10px;  font-size: 1.7em;  color: #fff;  text-transform: uppercase; letter-spacing: 2px;  font-weight: bold; }
      #dropdowns { padding-left:10px;  font-size: 1em;  color: #fff;  letter-spacing: -1px;  }
      .others { text-decoration:none; float: left; padding-left:8px;  font-size: 1em;  color: #fff; letter-spacing: -1px; }
      #home-container {  margin-top: 25px; }
      link {     text-decoration: none; }
      #page-content { flex: 1 0 auto ; }
      body {background-color: azure; 
        min-height: 100vh;
    display: flex;
    flex-direction: column;
      }
      .home {
        background-image: url('https://images.unsplash.com/photo-1432821596592-e2c18b78144f?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=870&q=80');  
        background-position: center center; 
        background-repeat: no-repeat;  
        background-size: cover; 
        height: 400px;
      }
      .button_to {
        display: inline;
      }

      .button_to div {
        display: inline;
      }
      .imgs {
        max-width: 100%;
        height: auto;
        display: block;
        background-color: white;
        box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2), 0 6px 20px 0 rgba(0, 0, 0, 0.19);
        border: 1px solid #ddd;
        border-radius: 4px;
        padding: 5px;
      }
      .logo-img {
        max-width: 100%;
        height: auto;
        display: block;
        background-color: white;
        border: 1px solid #ddd;
        border-radius: 4px;
        padding: 5px;
      }

      tr:nth-child(even) {background-color: #f2f2f2;}
      .footer {
        position: relative;
        bottom: 0;
        width: 100%;
        margin-top: auto;
      }
      .hidden { display: none;}
      .width { width: 100px; }    
      li > .nav-link {
        display: inline-block;
      }  
      .footer-custom{
        height:40px
      }
</style>