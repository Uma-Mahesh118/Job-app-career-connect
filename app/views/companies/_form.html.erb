<div class="container text-center">
    <div class="row justify-content-center">
        <div class="col-10">
            <%= render 'shared/errors', obj: @company %>
            <%= form_with(model: @company, class: "shadow p-3 mb-3 bg-info rounded", local: true) do |f| %>
                <div class="form-group row">
                    <div class="col-2">
                    <%= f.label :name , {:class=> "col-2 col-form-label text-light"}%> <br/>
                    </div>
                    <div class="col-10">
                        <%= f.text_field :name,  {:class=>"form-control shadow rounded", :placeholder=>'Enter a Name'}%>
                    </div>
                </div>
                <br>
                <div class="form-group row">
                    <div class= "col-2">
                        <%= f.label :email , {:class=> "col-2 col-form-label text-light"}%> <br/>
                    </div>
                    <div class="col-10">
                        <%= f.email_field :email, {:class=>"form-control shadow rounded", :placeholder=>'Enter your email'} %>
                    </div>
                </div>
                <br>
                <div class="form-group row">
                    <div class="col-2">
                        <%= f.label :password , {:class=> "col-2 col-form-label text-light"}%> <br/>
                    </div>
                    <div class="col-10">
                        <%= f.password_field :password, {:class=>"form-control shadow rounded", :placeholder=>'Enter the password'} %>
                    </div>
                </div>
                <div>
                    <%= f.submit (@company.new_record? ? "Sign-Up!" : "Update Profile" ) , class: "btn btn-primary btn-lg shadow" %>
                </div>
            <% end %>
        </div>
        <div class="mb-10">
            <%= link_to '[ Cancel and return to companies listing ]', companies_path, {:class=> "btn btn-outline-info" , :style=> "background-color:'white'"} %>
        </div>
    </div>
 </div>
